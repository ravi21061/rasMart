const express = require("express");
const router = express.Router();

// customer routes 

router.post("/profile", () => { });

router.post("/address", () => { });

router.post("/orders", () => { });

router.post("/wishlist", () => { });

module.exports = router;